/** @type {import('next').NextConfig} */
const nextConfig = {
  // appDir is now stable in Next.js 14, no need for experimental flag
  // Test files are excluded via tsconfig.json exclude
}

module.exports = nextConfig

